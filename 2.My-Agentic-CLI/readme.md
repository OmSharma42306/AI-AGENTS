# Om'S AI AGENT

- So this Is a CLI Agent Inspired by Claude Code CLI.
- So It can Build Websites by Your Just Prompt only.
- prompt : Can you create a basic todo app using HTML, CSS, and JS? Use the `executeCommand` tool to write the files to disk. Create 3 files: index.html, style.css, script.js."
- so it's a free model so you need to give some specific topic , if you use OPENAI model , it just can generate by generate a todo app. it would build a app.